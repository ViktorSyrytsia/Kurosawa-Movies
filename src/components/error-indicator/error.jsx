import React from 'react';

import './error.scss';

const Error = ({ err }) => {
        console.log(err);

        return (
                <div>
                        Error!
                </div>
        )
}

export default Error
